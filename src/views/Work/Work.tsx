import React from 'react';

import { Container } from '@components/index';
import { MENU } from '@constants/index';

const Work: React.FC = (): JSX.Element => {
  return (
    <Container id={MENU.WORK}>
      Work View
    </Container>
  )
}

export default Work;
